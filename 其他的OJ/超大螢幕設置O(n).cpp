#include<bits/stdc++.h>
using namespace std;
int main(){
	ios::sync_with_stdio(0);
	cin.tie(0);
	
	stack<int> st;
	int n,x,i,w,h,mx;

	st.push(0);
	cin >>n;
	for(i=0;i<n;i++){
		cin >>x;
		w=1;
		mx=0;
		if(x<st.top())st.push(x);
		while(x<st.top()){
			
		}
	}	
	
	
	
}

